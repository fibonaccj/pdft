kind = "web"
previewPath = "/"
title = "PDF Translator"
version = "1.0.0"
id = "artifacts/pdf-translator"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19255

[services.development]
run = "pnpm --filter @workspace/pdf-translator run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pdf-translator", "run", "build" ]
publicDir = "artifacts/pdf-translator/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19255"
BASE_PATH = "/"
